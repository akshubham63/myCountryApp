import { CountryDetailsShowDirective } from './country-details-show.directive';

describe('CountryDetailsShowDirective', () => {
  it('should create an instance', () => {
    const directive = new CountryDetailsShowDirective();
    expect(directive).toBeTruthy();
  });
});
